# Kept in git so the DuckDB target directory exists in a fresh clone. sqlfluff's dbt templater
# opens a connection in order to build the adapter, so linting needs this path to be openable
# even before anything has been built. The .duckdb files themselves are gitignored.

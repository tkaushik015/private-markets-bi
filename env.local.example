# 复制为 .env（或 .local/.env）——两者都被 .gitignore 排除；真实 key 永不入库
DEEPSEEK_API_KEY=
# 可选覆盖（默认值见 quantai/config/schema.py）：
# QUANTAI__DISTILL__MODEL=deepseek-v4-pro   # 预算紧可切 deepseek-v4-flash
# QUANTAI__LLM__MODEL_NAME=Qwen/Qwen3-8B

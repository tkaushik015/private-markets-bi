# Streamlit Community Cloud installs this file from the repo root by default.
# Keep it in lockstep with app/requirements.txt.
streamlit>=1.40
plotly>=6.0,<8
pandas>=2.2
duckdb>=1.1,<2
pyyaml>=6.0
pyarrow>=16.0

# Marts layer, Phase 2: tables with a declared grain, an enforced contract, uniqueness and
# not-null tests on that grain, and YAML docs stating each formula in plain English.
# Placeholder so the layout is committed before any model exists. Delete once populated.

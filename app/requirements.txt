# Streamlit Community Cloud installs this file. Keep it thinner than the full
# package extras: the public deploy reads the committed DuckDB demo and does not
# need dbt, Snowflake or the generator.
streamlit>=1.40
plotly>=6.0,<8
pandas>=2.2
duckdb>=1.1,<2
pyyaml>=6.0
pyarrow>=16.0
